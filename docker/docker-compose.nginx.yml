version: '3.8'

services:
  app:
    build:
      context: ..
      dockerfile: docker/Dockerfile.nginx
    image: mysigmail-static-nginx:latest
    container_name: mysigmail-app-nginx
    restart: unless-stopped
    ports:
      - "80:80"
    # volumes:
    #   - ./deploy/nginx/nginx.conf:/etc/nginx/conf.d/default.conf
